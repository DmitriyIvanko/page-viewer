export const WIDTH_EPS = 0.01;

export const PRELOAD_PAGES_COUNT = 1;
export const INITIAL_ZOOM = 1;

// assume this is page view/height the same for every page and can not be changed by user:
export const PAGE_WIDTH_PX = 794;
export const PAGE_HEIGHT_PX = 1_123;

// empirical const for smooth behavior:
export const MIDDLE_STRATEGY_SHIFT = 0.24;
export const END_STRATEGY_SHIFT = 0.34;

